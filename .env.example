# ============================================
# 🔐 VARIABLES DE ENTORNO - Food Orders CRM
# ============================================

# 📌 Clerk Authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your-clerk-publishable-key
CLERK_SECRET_KEY=your-clerk-secret-key
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/login
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/login
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# 📌 Database
DATABASE_URL="postgresql://user:password@localhost:5432/food_crm?schema=public"

# 📌 Cron Job Security (NUEVO - Para órdenes recurrentes)
# Genera un secret aleatorio con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your-super-secret-random-string-here

# 📌 Next.js
NEXT_PUBLIC_API_URL=http://localhost:3000

# ============================================
# 🚀 INSTRUCCIONES:
# ============================================
# 1. Copia este archivo a .env.local
# 2. Reemplaza los valores con tus credenciales reales
# 3. Para generar CRON_SECRET ejecuta en terminal:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Nunca subas el archivo .env.local a Git
# ============================================
